<UserControl x:Class="Quickr.Views.Server.ServerInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quickr.Views.Server"
             xmlns:viewModels="clr-namespace:Quickr.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance viewModels:ServerViewModel}">
    <Grid>
         <TabControl BorderThickness="0">
             <TabControl.Resources>
                 <Style TargetType="TabItem">
                     <Setter Property="Padding" Value="35,5" />
                 </Style>
             </TabControl.Resources>
             <TabItem Header="Info" Margin="0,0,0,-1">
                 <local:KeyValueGroupView DataContext="{Binding Info}"/>
             </TabItem>
             <TabItem Header="Config" Margin="-1,0,0,-1">
                 <local:KeyValueGroupView DataContext="{Binding Config}" />
             </TabItem>
         </TabControl>   
    </Grid>
</UserControl>
